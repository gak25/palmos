<!-- react-app -->

<!-- <p id="notice"><%= notice if notice %></p>
<p id="alert"><%= alert if alert %></p>
<%= form_for :session, url: sign_in_path do |f| %>
  <i class="fa fa-lock fa-4x" id='lock-icon' aria-hidden="true"></i>
  <div id='register'>Sign In</div>
  <div class='sign-in-error'><%= flash[:alert] %></div>
  <div class="form-inputs">
    <fieldset>
      <%= f.text_field :login, placeholder: "email or username", autoComplete: "off", autoFocus: "on" %>
    </fieldset>
    <fieldset>
      <%= f.password_field :password, placeholder: "password", autoComplete: "off" %>
      <label>
        <%= link_to "Forgot your password?", new_password_reset_path %>
      </label>
    </fieldset>
    <fieldset>
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me, class: "switch-paddle" do %>
        <span class="show-for-sr">Remember me on this device</span>
      <% end %>
    </fieldset>
  </div>
  <div class="form-actions">
    <%= f.submit "Sign In", class: "button" %>
    &nbsp;
    <%= link_to "Sign Up", sign_up_path, class: "button" %>
  </div>
<% end %> -->
